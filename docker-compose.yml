version: '3.8'
services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app
      - ./db:/db
    # --reload をつけることで開発を効率化
    command: uvicorn app.main:app --host 0.0.0.0 --reload